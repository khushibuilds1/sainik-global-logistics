'use client'

// This component is currently disabled.
// We are keeping the file so you can use the section again later if needed.

export function WhyChooseUs() {
  // Returning null means:
  // - Nothing will be displayed on the website
  // - No empty space will be created
  // - The component will not cause any UI changes

  return null
}